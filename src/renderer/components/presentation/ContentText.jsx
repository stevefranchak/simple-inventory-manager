import styled from 'styled-components';
import { colors } from '../../styles/constants';

const ContentText = styled.p`
  margin: 0;
  color: ${colors.SUPPORTING_TEXT}
`;

export default ContentText;
